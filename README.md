# CLIP Image-Text Search

A simple multimodal retrieval system using CLIP embeddings for text-to-image semantic search.

## Goal
Build a mini multimodal pipeline that:
- loads images
- embeds images with CLIP
- embeds text queries with CLIP
- compares embeddings in a shared space
- retrieves the most relevant images for a query
